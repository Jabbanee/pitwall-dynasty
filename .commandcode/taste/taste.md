- Prefers brief-driven recovery sessions to be handled with full local-state preservation: never `git reset --hard`, `git checkout .`, `git restore .`, `git clean -fd`, or `git pull --rebase` before inspecting the working tree, diff, and history. Treat local working tree as the primary recovery source. Confidence: 0.95
- Wants staging of accidental artifacts (e.g. `commit_msg.txt`, `append-css.cjs`, `temp-*` scripts) blocked via `.gitignore` rather than removed per-incident; ad-hoc files should never be committed in the first place. Confidence: 0.9
- Strongly dislikes "ugly compile-silencing hacks" such as `void driverEco`, `void 0 as unknown as ...`, `as Record<string, X>` casts used only to placate the type system. Wants clean explicit type fixes and proper route matching instead. Confidence: 0.95
- Prefers a single canonical, reusable talent-generation path over duplicated generators in `core/content.ts` and `championship/engine.ts`. Duplicates are a bug to consolidate. Confidence: 0.9
- Requires that all canonical generators be gender-neutral: same seed + same config with different `gender` must produce equivalent talent distributions. Gender is identity/context only and must NEVER affect potential, pace, ability caps, development rate, peak ability, AI valuation, contract acceptance, licence eligibility, or promotion likelihood. Confidence: 0.99
- Wants centralised historical/era-based gating (e.g. `womenSeriesEstablished`, `eraYear >= 2014`) instead of hardcoded checks scattered through the game; one place to read, one place to flip. Confidence: 0.9
- Prefers stable fictional series IDs and names (`base.junior.regional`, `base.junior.continental`, `base.junior.aurora`, `base.championship.wgp`). Real-world branding (F1/F2/F3/F1 Academy etc.) must not be substituted. Confidence: 0.95
- Wants explicit, deterministic seeded tests for fairness/gender-neutrality properties instead of weak "average over random samples" assertions. The bar is: female driver can reach potential ≥ 90, female pace ceiling == male pace ceiling, scouting tiers ignore gender, offers ignore gender, licence ignores gender, deterministic elite female career path succeeds. Confidence: 0.95
- Hates flakiness in test fixes: will not accept arbitrary `waitFor` lengthening or "rerun and it passed" as a fix. Real nondeterminism must be diagnosed and fixed properly. Standard diagnosis: run the failing test in isolation, then with the neighbouring test file, then 5x in a row to distinguish cross-test contamination from a one-off transient; the root cause for a rarely-failing test is almost always unseeded randomness in production code (e.g. `Date.now()`-derived championship seed) that the test does not pin. Confidence: 0.9
- Hardcoded version strings in UI (e.g. literal "1.0.0" in title screen while `package.json` says "0.1.0") are a bug, not a nit. Version must come from `package.json` / `app.getVersion()` over the desktop bridge with a sensible fallback. Never duplicate version constants manually. Confidence: 0.9
- A setting that exists in the settings store must actually be honored by the rendering / simulation loop. If a settings field is wired to no consumer (e.g. `fpsLimit` set but never read by the renderer), it is a defect to be fixed in this pass, not documented as "future work". Audit every field. Confidence: 0.9
- Three.js / WebGL lifecycle in long-running desktop sessions: on broadcast unmount, traverse the scene and dispose every material + geometry, then drop the WebGL context via `WEBGL_lose_context`. Do not let a returning broadcast inherit a leaked renderer. Confidence: 0.85
- User-specific tool/agent config files (e.g. `.commandcode/`) are not project artefacts: even if previously committed, they should be unstaged in this pass's commit so the work-tree state stays portable across machines. Confidence: 0.8
- Requires the test/build gate sequence at the end of every significant pass: `npx tsc --noEmit`, `npx vitest run`, `npm run build` — all must pass — followed by the existing authoritative multiplayer two-client regression. Confidence: 0.95
- Prefers clean, focused, atomic commits with conventional-commit style prefixes (`feat(ui):`, `feat(career):`, `test(drivers):`, `docs(qa):`); never squash or rewrite the existing local commit history unnecessarily. Confidence: 0.9
- Mandates that all local work be pushed to GitHub by the end of the pass, not left in a local-only state. The final state must show `0 0` ahead/behind relative to origin/master. Confidence: 0.95
- Wants screenshot-based visual QA gated by a "kill stale frontend processes → fresh build → hard reload → verify the feature is visible → capture" restart procedure. Stale preview output is not acceptable evidence. Confidence: 0.9
- Communicates in Finnish when delegating long, structured passes. The working language for multi-step task briefs is Finnish. Confidence: 0.85
- Prefers that interrupt/recovery passes explicitly enumerate the previous pass's CRITICAL state (starting HEAD, origin SHA, recovered commits, recovered uncommitted work) in the final report. Confidence: 0.85
- Believes the driver-development facility, scouting engine, academy, reserve, and licence system should be wired into the same loop and not remain "UI-only" placeholders. Visual existence without backing logic is a deficiency. Confidence: 0.9
- Dislikes TypeScript "any / unknown as" escape hatches used to silence unused-import or unused-symbol errors; the correct fix is removing the import or actually using the symbol. Confidence: 0.9
- Wants route matching in the SPA router to use explicit `hash.startsWith('#/series/')` / `hash.startsWith('#/driver/')` style checks for dynamic routes, not duplicate `case` arms. Duplicate cases are a real bug. Confidence: 0.9
- Wants feeder ecosystem drives, watchlist, and scouting to be a single canonical pipeline reused by both player and AI teams — no parallel "AI-only" talent system. Gender-neutrality must hold for AI recruitment. Confidence: 0.9
- Wants narrative/historical-fiction framing: era-based events (e.g. "NEW WOMEN'S DEVELOPMENT CHAMPIONSHIP ANNOUNCED" with a deterministic season stamp) are preferred over silent flag flips. Empty state UI should reflect "not yet established" rather than crash. Confidence: 0.85
- Wants save-migration versions to be deterministic: v2→v3 must assign `gender` and other new fields reproducibly across reloads, with round-trip tests. Confidence: 0.9
- Dislikes starting adjacent work (Electron/Tauri/desktop packaging) until the current implementation pass is fully stable and pushed. Explicit "DO NOT begin Electron/desktop packaging" calls are common. Confidence: 0.95
- When the user provides a 100-point structured pass brief, treat it as a contract: every numbered point must be addressed in the implementation or in the final report's Known Limitations. The final report's required sections (DESKTOP ARCHITECTURE, ELECTRON SECURITY, GAME WINDOW, DISPLAY/GRAPHICS SETTINGS, AUDIO SETTINGS, SAVE SYSTEM, AUTOSAVE, LOAD/CONTINUE, LOGGING/CRASH HANDLING, OFFLINE MODE, MULTIPLAYER, PACKAGING, WINDOWS INSTALLER, CLEAN-MACHINE VERIFICATION, STANDALONE GAMEPLAY QA, VISUAL QA, TESTS, ARTIFACTS, GIT, KNOWN LIMITATIONS) must each be present in the closeout, even if briefly. Confidence: 0.9
- The final action of a desktop / standalone-conversion pass is to leave the actual packaged artifact (or its dev-mode equivalent when GUI is unavailable) running on a polished gameplay screen with no frontend dev server, terminal, or browser chrome visible. Reporting the work as done while still on a `localhost:5175` browser is a fail. Confidence: 0.9
- For visual QA on a packaged build that the CI cannot launch: render the same UI surface in the browser at the matching pixels (the packaged renderer is byte-identical to the browser build) and commit those screenshots, clearly labelled as the desktop-pc-pass set. Never claim a packaged-build screenshot exists when only a browser screenshot was captured. Confidence: 0.85
- Era-aware visual systems (car chassis, broadcast chrome, track safety, tyre compounds) are a structural concept, not a label: a single `eraFactor ∈ [0..1]` resolves deterministically into a small set of family buckets (1980s / early-90s / late-90s / 2000–2008 / 2009–2013 / 2014–2021 / 2022+) and every per-era attribute (halo presence, ground-effect floor, rear-wing height, sidepod width, tyre proportions, era-correct runoff / barriers) is derived from that factor. The public visual interface exposes a readonly `eraDimensions` / era-fingerprint field so tests can assert `1980 ≠ halo, 2022 = halo, era dimensions differ, era family resolves deterministically` without reaching into Three.js internals. Tests must assert the structural property, not the rendered pixel. Confidence: 0.9
- Director / camera / event-queue systems (TV Director, replay director, race control) are local renderer concerns. They use a tiered priority queue (CRITICAL → HIGH → NORMAL) backed by a sticky-event TTL so stale events (race-start that fired 30 s ago) auto-drop and the broadcast cadence (minimum switch interval, ~2 s) never chatters. The director must never participate in the authoritative wire protocol — each client runs its own director and may watch a different camera over the same authoritative race. CRITICAL events cut; lower-priority transitions smoothly pan. Confidence: 0.9
- Graphics-quality presets (LOW / MEDIUM / HIGH / ULTRA) are not just a label on a settings field — each preset must produce visibly different scene complexity (vegetation density, crowd block count, shadow / spray / particle counts, draw-call count) and the change must be measurable at the rendered scene. HIGH is the intended default. Simulation tick rate and authoritative state are invariant to the preset. Document the per-preset differential in code so QA and the next pass can see at a glance what changes. Confidence: 0.9
- Reusable fictional environment themes (FOREST / MOUNTAIN / COASTAL / DESERT / URBAN-PARK / MODERN-PURPOSE-BUILT) drive terrain, vegetation, sky, fog, structures, and palette in a single shared `EnvironmentTheme` table. The theme is picked from a circuit's characteristics + a stable hash of the circuit id so the same circuit always produces the same theme. Never hardcode circuit-specific logic in the renderer; never reproduce a recognizable real circuit (Spa, Monza, Silverstone, Suzuka, Monaco, Interlagos, etc.) — fictional circuits may evoke broad archetypes only. Confidence: 0.95
- A "real commercial motorsport game" pass has a fixed final action: leave the actual packaged executable running in a live race on a fully scened fictional circuit, with the new TV director/camera system driving the broadcast, and no Vite or browser anywhere. Reporting the work done while still on a `localhost:5175` browser or while the game is on the title screen is a fail. The packaged `.exe` must demonstrate the new feature in motion. Confidence: 0.95
- Strict layered separation for 3D world work: AUTHORITATIVE SIMULATION ≠ TRACK PRESENTATION ≠ CAR PRESENTATION ≠ CAMERA DIRECTOR ≠ UI/BROADCAST GRAPHICS. Each layer has its own data definition; simulation never reads from presentation, presentation never feeds back into simulation. Track elevation, car pose, and visual position are presentation-only; sim progress stays deterministic. Centreline drives presentation mapping, not the other way around. Confidence: 0.95
- Graphics-quality presets (LOW / MEDIUM / HIGH / ULTRA) must produce visibly different scene complexity — reduced crowd / low vegetation / no spray / minimal shadows at LOW, maximum scenery density and fuller crowd/vegetation at ULTRA. A preset that does not change the rendered scene is a defect; HIGH is the intended default, and presets must never feed back into simulation. Confidence: 0.9
- F1-style compound color convention is the default for readable motorsport UI: SOFT red, MEDIUM yellow, HARD white, INTERMEDIATE green, WET blue. Do not copy branded Pirelli markings, logos, or naming. The functional color encoding is industry standard and acceptable for a fictional championship. Confidence: 0.9
- Circuit world-building uses reusable fictional environment themes (FOREST / MOUNTAIN / COASTAL / DESERT / URBAN-PARK / MODERN-PURPOSE-BUILT) layered on top of unique per-circuit layout and object placement. Each theme drives terrain, vegetation, sky/fog, structures, and palette. Never hardcode circuit-specific logic in the renderer; never reproduce a recognizable real circuit (Spa, Monza, Silverstone, Suzuka, Monaco, Interlagos, etc.) — fictional circuits may evoke broad archetypes only. Confidence: 0.95
- Era-family system for cars (1980s, early 1990s, late 1990s, 2000–2008, 2009–2013, 2014–2021, 2022+) is a structural concept, not a label. Each era resolves to deterministic geometry differences: halo presence from 2014 onward, period-correct nose / sidepod / wing / engine cover proportions, period-correct tyre proportions, period-correct track safety (older eras = less asphalt runoff, more gravel, simpler barriers). Tests must assert 1980 ≠ halo, 2022 = halo, era dimensions differ, era family resolves deterministically. Confidence: 0.9
- TV Director priority is tiered, not flat: CRITICAL (incident, race start/finish, player pit event) → HIGH (close battle, overtake, pit stop, leader fight) → NORMAL (leader, player driver, interesting group). Avoid constantly changing camera for tiny events; the director owns the broadcast cadence and is local to each client (player A may watch driver A1 onboard while player B watches leader, both over the same authoritative race). Camera transitions support CUT / SMOOTH PAN / BLEND — not chaotic switching. Confidence: 0.9
- Circuit visual QA gate: at least three circuits must be visually distinct (forest / mountain / coastal-desert-modern) and recognizable from a single screenshot without reading the name. Layout, elevation, surroundings, track width, corner character, runoff, pit complex, color palette, and scenery must differ in combination. Screenshot directories follow `docs/testing/screenshots/<phase>/` with a fixed canonical numbered list per pass; final representative screenshots come from the actual packaged `.exe`, not just the browser. Confidence: 0.9
- After a flaky-test diagnosis, the fix must be a proper architectural change (e.g. `MultiplayerLobby` accepts an optional explicit seed; tests pin a deterministic fixed seed; production continues to generate an unpredictable session seed) — followed by 10 consecutive deterministic runs of the previously flaky test, all of which must pass 10/10, plus the full suite. Documenting the flakiness and moving on is not an acceptable resolution. Confidence: 0.95
